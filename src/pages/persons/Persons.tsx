import { FC } from 'react';

interface PersonsProps {}

const Persons: FC<PersonsProps> = ({ }) => {
    return (
        <>
            <h1>Persons</h1>
        </>
    );
}

export default Persons;
